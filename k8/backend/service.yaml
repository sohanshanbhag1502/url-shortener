apiVersion: v1
kind: Service
metadata:
  name: url-shortener-backend-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: url-shortener-backend
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
